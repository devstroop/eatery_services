name: eatery_services
description: A new Flutter project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.17.2 < 3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  path_provider: ^2.0.11
  path: ^1.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
